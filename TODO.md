- [ ] Explain current flow: customer Menu uses /api/products and fallback products; rendering currently uses a placeholder emoji instead of product.image_url
- [ ] Update customer Menu UI to display product photo using product.image_url (with /uploads static base)
- [ ] Ensure image_url is correctly prefixed for the customer app (handle relative paths)
- [ ] Add graceful fallback when image_url missing or product unavailable
- [ ] Run frontend/backend build or typecheck and verify images load

